<Window x:Class="Hotel.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Hotel"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Button Click="Button_Click" Content="Button" Width="526" Margin="10,0,264,379"/>

        <ListView Name="myListView" Margin="0,60,0,0">

            <ListView.View >
                <GridView>
                    <GridViewColumn Header="Imie" DisplayMemberBinding="{Binding Imie}"/>
                    <GridViewColumn Header="Nazwisko" DisplayMemberBinding="{Binding Nazwisko}" />
                    <GridViewColumn Header="Telefon" DisplayMemberBinding="{Binding Telefon}" />
                    <GridViewColumn Header="Pobyty" DisplayMemberBinding="{Binding Pobyties}" />
                    <GridViewColumn Header="Uslugi" DisplayMemberBinding="{Binding WykupioneUslugis}" />
                </GridView>

            </ListView.View>
        </ListView>

    </Grid>
</Window>
